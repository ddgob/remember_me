services:
  web:
    command: gunicorn --bind 0.0.0.0:8000 --workers 3 lembra_ai.wsgi:application
    env_file:
      - .env.prod
    ports:
      - "8000:8000"
    environment:
      - DJANGO_DEBUG=0
      - DJANGO_SETTINGS_MODULE=lembra_ai.settings.production